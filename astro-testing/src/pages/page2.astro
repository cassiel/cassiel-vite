---
import { getCollection /*, getEntry*/ } from 'astro:content';
const posts = await getCollection('blog');

interface Post {
    data: { title: string }
}
---

<h1>TEST</h1>
<ul>
    {posts.map((post: Post) => <li>{post.data.title}</li>)}
</ul>
